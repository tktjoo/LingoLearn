--- app/build.gradle.kts
+++ app/build.gradle.kts
@@ -16,11 +16,6 @@
         vectorDrawables {
             useSupportLibrary = true
         }
-        ndk {
-            // Azure Speech SDK 1.38.0 does not align x86/x86_64 to 16KB yet.
-            // Exclude these ABI architectures from the build to prevent install errors on Android 15 emulators/devices.
-            abiFilters += listOf("armeabi-v7a", "arm64-v8a")
-        }
     }

     buildTypes {
